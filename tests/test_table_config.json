{
    "type": "metric",
    "create_fields": true,
    "columns": {
        "id": {
            "fields": [
                "lead_id",
                {"name": "leads", "ds_formula": "COUNT(DISTINCT leads.id)"}
            ]
        },
        "created_at": {
            "allow_type_conversions": true
        },
        "campaign_id": {
            "fields": ["campaign_id"]
        }
    }
}
